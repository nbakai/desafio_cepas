<%= form_with(model: enologo, local: true) do |form| %>
  <% if enologo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enologo.errors.count, "error") %> prohibited this enologo from being saved:</h2>

      <ul>
      <% enologo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :edad %>
    <%= form.number_field :edad %>
  </div>

  <div class="field">
    <%= form.label :nacionalidad %>
    <%= form.text_field :nacionalidad %>
  </div>
 
    <div class="field">
      <%= form.label :revist, 'Revista' %>
      <%= form.collection_check_boxes(:revist_ids, Revist.all, :id, :nombre) do |b| %>
        <%= b.label class:"label-checkbox" do%>
          <%=b.check_box + b.text %>
         <%= fields_for :enologo_revist do |f| %>
        <%= f.select :cargo, options_for_select(['Editor', 'Writer', 'Reviewer'])%>
        <%end%>
      <%end%>
    </div>
  <%end%>

    
  
  <div class="actions">
    <%= form.submit %>
  </div>
 
<% end %>
