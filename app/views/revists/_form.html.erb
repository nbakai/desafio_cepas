<%= form_with(model: revist, local: true) do |form| %>
  <% if revist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revist.errors.count, "error") %> prohibited this revist from being saved:</h2>

      <ul>
      <% revist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
